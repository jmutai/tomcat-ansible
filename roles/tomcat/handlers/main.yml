---
- name: restart tomcat
  ansible.builtin.systemd_service:
    name: "{{ tomcat_service_name }}"
    state: restarted
    daemon_reload: true

- name: reload tomcat
  ansible.builtin.systemd_service:
    name: "{{ tomcat_service_name }}"
    state: reloaded
